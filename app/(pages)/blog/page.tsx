const Blog = () => {
  // const postList = posts.map((post) => (
  //   <div key={post.title}>
  //     <Link href={`/blog/${post.slug}`}>{post.title}</Link>
  //   </div>
  // ));

  return <div className="mt-[61px]">Ovo je blog</div>;
};

export default Blog;
